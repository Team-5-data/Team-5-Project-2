Week-2 Task

The second week focuses on data querying and metric extraction. The intern will import the cleaned dataset into a SQL database and write complex queries to answer the core business questions. This includes using GROUP BY, ORDER BY, and aggregate functions (SUM, AVG) to calculate Total Revenue, Best-Selling Products by Volume, and Geographic Sales Distribution. All SQL scripts must be committed to GitHub with clear, semantic commit messages.
