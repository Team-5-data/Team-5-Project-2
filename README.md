Week-1 Task

The inaugural week demands the establishment of the foundational infrastructure. Interns must initiate the GitHub repository, configuring it to track their Jupyter Notebooks and SQL scripts. The intern will import the raw retail dataset into Python/Excel to begin the Data Cleaning phase. Tasks include identifying and handling NULL values, removing duplicate records, standardizing date/time formats, and validating data types across all columns.
